# Loops.so API key — Bearer token.
# Get it from Loops Settings -> API. Never commit the real key.
LOOPS_API_KEY=

# (Optional) Override the who-can-send allowlist in references/senders.md
# as a comma-separated list of email addresses.
# NEWSLETTER_SENDERS=you@example.com,teammate@example.com